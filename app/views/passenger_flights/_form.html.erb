<%= form_for(@passenger_flight) do |f| %>
  <% if @passenger_flight.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@passenger_flight.errors.count, "error") %> prohibited this passenger_flight from being saved:</h2>

      <ul>
      <% @passenger_flight.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :clase, "Clase" %><br>
    <%= f.text_field :clase %>
  </div>
  <div class="field">
    <%= f.label :asiento, "Asiento" %><br>
    <%= f.text_field :asiento %>
  </div>
  <div class="field">
    <%= f.label :passenger_id, "Rut pasajero" %><br>
    <%= f.text_field :passenger_id %>
  </div>
  <div class="field">
    <%= f.label :flight_id, "ID vuelo" %><br>
    <%= f.text_field :flight_id %>
  </div>
  <div class="actions">
    <%= f.submit "Agregar Pasajero/Vuelo" %>
  </div>
<% end %>
